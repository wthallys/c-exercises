#include <stdio.h>

int main() {
    char nome[49];
    while (1)
    {
        scanf("%s", nome);
        if (nome == "sair")
            break;
        else
        {
            int count = 0;
            float salto = 0;
            while (salto >= 0)
            {
                /* code */
            }
        }
        
    }

    do
    {
        scanf("%s", nome);
    } while (nome != "sair");
    
    

    return 0;
}